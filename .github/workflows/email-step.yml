name: Email Step Demo

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 23f3003750@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions!!"
